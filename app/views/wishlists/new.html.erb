<div class = "new_wishlist center">
	<h2 class="header center orange-text">Wishlist</h2>

	<%= render 'form' %>

	<%= link_to 'Back', user_wishlist_path(current_user.id), class: "waves-effect orange btn" %>
</div>