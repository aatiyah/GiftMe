<% if activity.trackable %>
	<% event_title = activity.trackable.title %>
	<% event_link = activity.trackable %>

	<i class="material-icons" >add_circle_outline</i>

	added an event
	<%= link_to event_title, event_link %>. 
<% else %>
	added an event, which has since been removed.
<% end %>