<div class="card" id="search-card">
	<div class="search center">
		<%=form_tag products_search_path, :method => 'get' do %>
			<p>
				<%= text_field_tag :search, params[:search] %>
				<%= submit_tag "Search", class: "waves-effect orange btn", :name => nil %>
			</p>
		<% end %>
	</div>
</div>